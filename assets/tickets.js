(()=>{const S=Sanchari,items=S.saved(),el=document.getElementById('savedTickets');if(!items.length){el.innerHTML='<div class="empty-state">No bookings saved here yet.<br>Your next journey will appear here.</div><a class="primary-btn" href="index.html">Explore bus routes</a>';return}for(const item of items){const a=document.createElement('a');a.className='saved-ticket';a.href=S.recovery(item);const name=item.route==='miya'?'Miyapur':'Patancheru';const route=item.direction==='to'?name+' → IITH':'IITH → '+name;a.innerHTML='<strong>'+S.escape(route)+'</strong><small>'+S.escape(item.departure?S.when(item.departure*1000):'Check payment status')+' · Open booking ↗</small>';el.append(a)}})();
